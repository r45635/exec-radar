[project]
name = "exec-radar-collectors"
version = "0.1.0"
description = "Job collector interfaces and implementations for Exec Radar"
requires-python = ">=3.12"
dependencies = [
    "httpx>=0.27.0",
    "exec-radar-schemas",
]
